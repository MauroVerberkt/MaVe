# Monads

Functional primitives for C# with `Result` and `Option` types.

## Package

- NuGet: `Monads`

## Highlights

- Explicit success/failure flow with `Result`
- Nullable-safe optional values with `Option`
- Functional composition helpers (`Map`, `Bind`, `Match`)
